repo1 
 first repository of source code that will contain scripts from windows land.
 these scripts will have their own documentation. 
 this readme will get a summary of all the files contained within it.

toggleSSLinIIS.bat
 This batch script enables an administrator to serve content 
 only over SSL from the command line

setDateAsEnvironmentVariable.bat
 Sets date in YYYY-MM-DD format as an environment variable
